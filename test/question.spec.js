import {
    mount
} from 'vue-test-utils';
import expect from 'expect';
import Question from '../src/components/CouponCode.vue';

describe('Question', () => {
   
    let wrapper;
    beforeEach( () => {
        wrapper = mount(Question);
    });

    it('presents the title and the body', () => {
        
    })
});